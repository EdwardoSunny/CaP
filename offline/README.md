# LMP Code Gen Test

Use the script in this folder with the following steps:

1. Source the `uv` environment in the repo.
2. Edit the context so it contains a comma-separated list of available objects.
3. Run (in the root of the repo):

```bash
python offline/lmp_codegen_test.py "task"
```
